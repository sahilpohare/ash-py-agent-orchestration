"""
Components - shadcn-style code templates that get copied into your project.

Each component is a directory with:
    manifest.json  - metadata, dependencies, target paths
    files/         - the actual source files to copy
"""
